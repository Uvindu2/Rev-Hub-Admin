export interface CustomerResponseProjection {
  customerId: number;
  customerName: string;
  contactNumber: string;
  email: string;
  drivingLicenseNumber: string;
  active: boolean;
  customerAddress: string;
}
